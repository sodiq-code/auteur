# Auteur backend — Agentic Cinema Hackathon (Parallel Partner Track)
# Google-native only (blueprint Section 25 / Table 27). No OpenAI/Anthropic/LangChain.

# Google Agent Development Kit (REQUIRED by rules §7A — stack lock)
google-adk>=1.0.0

# Vertex AI / Gemini
google-genai>=2.0.0
google-cloud-aiplatform>=1.74.0

# Google Cloud infra
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.16.0
google-auth>=2.30.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# Data + HTTP
pydantic>=2.7.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Image / video composition (validation + assembly)
Pillow>=10.4.0

# CLI
typer>=0.12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
